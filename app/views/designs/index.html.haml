.container
	%h3 Designs
	%br
	%table{id: "designs"}
		%thead
			%tr 
				%th Date
				%th Customer
				%th Pickup On
				%th Remarks
		%tbody
			- @design.each do |d|
				%tr
					%td= d.created_at.strftime('%F')
					%td= link_to d.customer,d
					%td= d.pickup_date
					%td= d.remarks

	= link_to raw('<span class="fa fa-plus"></span> Add New'), new_design_path, class: "btn btn-primary"

:javascript
	jQuery(function($) {
		$('#designs').DataTable({
			sPaginationType: "full_numbers",
			bJQueryUI: true,
			bdestroy: true
			// sDom: '<"H"Tl>tS<"F"ip>'
		});
	});